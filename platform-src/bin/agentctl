#!/usr/bin/python3
"""Compatibility entrypoint for the packaged agentctl implementation."""

from pathlib import Path

_IMPLEMENTATION = Path(__file__).resolve().parents[1] / "agentdev" / "broker" / "cli.py"
exec(compile(_IMPLEMENTATION.read_bytes(), str(_IMPLEMENTATION), "exec"), globals(), globals())
